{
    "name": "@alfred/deploy",
    "version": "0.1.0",
    "private": true,
    "description": "Alfred's deployment engine — artifact transformation and Vercel deployment",
    "main": "./dist/index.js",
    "module": "./dist/index.mjs",
    "types": "./dist/index.d.ts",
    "files": ["dist"],
    "scripts": {
      "build": "tsup src/index.ts --format cjs,esm --dts",
      "dev": "tsup src/index.ts --format cjs,esm --dts --watch",
      "lint": "eslint src/",
      "typecheck": "tsc --noEmit",
      "test": "vitest run",
      "test:watch": "vitest",
      "clean": "rm -rf dist"
    },
    "dependencies": {
      "@alfred/core": "workspace:*"
    },
    "devDependencies": {
      "tsup": "^8.3.5",
      "vitest": "^2.1.8"
    }
  }